export default function OrgNotConnected() {
    return (
      <div className="h-full">
        <h1>
          You are not connected to an organization, contact the organization
          admin and retry login.
        </h1>
      </div>
    );
}